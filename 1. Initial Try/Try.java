public class Try {
    public static void main (String[] args){
        int[][] nums = new int[4][4];
        for (int i = 0; i < ; i++) { // Should get the index for each row
            for (int j = 0; j < ; j++) { // Should get the index for each column
                nums = (int) (Math.random() * 10); // Should assign the current value a random number
            }
        }

        for ( row : ) { // Should go through each row in the array
            for ( num : ) { // Should go through each value in the row
                System.out.println(); // Should print out each value
            }
        }
    }
}
